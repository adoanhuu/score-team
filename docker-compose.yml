services:
  app:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env.local
    ports:
      - "8788:8788"
    command: >
      sh -lc "npx wrangler@4.124.0 pages dev . --ip 0.0.0.0 --port 8788 --d1 DB=$$D1_DATABASE_ID"
