services:
  app:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env.local
    ports:
      - "8788:8788"
    command: >
      sh -lc "npx wrangler pages dev . --ip 0.0.0.0 --port 8788 --d1 DB=$$D1_DATABASE_ID"
